<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}Connecting Dots</title>
    {# Use the url filter for your stylesheet #}
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
    <script defer src="https://cloud.umami.is/script.js" data-website-id="393d0938-ebe6-402d-bdfc-357dcb84bcb9"></script>
</head>
<body>
    <header class="header">
        <div class="container">
            <h1 class="logo">
                <a href="{{ '/' | url }}">Connecting Dots</a>
            </h1>

            <nav class="nav" id="mobile-menu">
                <button class="close-btn" id="menu-close">×</button>

                <a href="{{ '/featured/' | url }}" class="nav-link {% if page.url == '/featured/' %}active{% endif %}">Featured</a>
                <a href="{{ '/posts/' | url }}" class="nav-link {% if page.url == '/posts/' %}active{% endif %}">All Posts</a>
                <a href="{{ '/about/' | url }}" class="nav-link {% if page.url == '/about/' %}active{% endif %}">About</a>
                <a href="{{ '/subscribe/' | url }}" class="nav-link {% if page.url == '/subscribe/' %}active{% endif %}">Subscribe</a>
            </nav>

            <button class="hamburger-btn" id="menu-toggle">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </header>

    <main class="main">
        <div class="container">
            {{ content | safe }}
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>Connecting the dots between what we think, who we are, and why we're here. <a href="https://iykb.kit.com/">Join the newsletter.</a></p>
            <p class="copyright">© 2025 Connecting Dots</p>
        </div>
    </footer>

    {# Use the url filter for your script file #}
    <script src="{{ '/js/script.js' | url }}"></script>
</body>
</html>